/*#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
void main()
{
	//사용자에게 현재 나이와 원하는 연도를 입력받고, 해당 연도의 나이를 출력하기
	int year, age, now, later;

	printf("현재 나이 ? ");
	scanf("%d", &age);
	printf("원하는 년도 ? ");
	scanf("%d", &year);
	now = 2024;
	later = year - now + age;
	printf("%d년의 나이 %d", year, later);
}*/